local Library = loadstring(game:HttpGet("https://pastebin.com/raw/vff1bQ9F"))()
local Window = Library.CreateLib("NEKO X TIENCF122", "DarkTheme")

local Tab1 = Window:NewTab("BẢN CẬP NHẬT")
local Tab2 = Window:NewTab("TRANG CHỦ")
local Tab3 = Window:NewTab("BLOX FRUIT")
local Tab4 = Window:NewTab("SĂN BOUTY!")
local Tab5 = Window:NewTab("BÀN PHÍM PC!")
local Tab6 = Window:NewTab("HACK THƯỜNG!")
local Tab7 = Window:NewTab("TĂNG FPS & XEM FPS!")
local Tab8 = Window:NewTab("NGƯỜI TẠO!")

local Tab1Section = Tab1:NewSection("THÊM TÍNH NĂNG & SỬA LỖI!")
local Tab2Section = Tab2:NewSection("COMING SOON!")
local Tab3Section = Tab3:NewSection("BLOX FRUIT!")
local Tab4Section = Tab4:NewSection("AIM SKILL + AIM GUN!")
local Tab5Section = Tab5:NewSection("HỖ TRỢ TẤT CẢ MAP!")
local Tab6Section = Tab6:NewSection("HỖ TRỢ TẤT CẢ MAP!")
local Tab7Section = Tab7:NewSection("TĂNG FPS & XEM FPS!")
local Tab8Section = Tab8:NewSection("TIENCF122!")

Tab3Section:NewToggle("HOHO HUB:FREE & PREMIUM X BLOX FRUIT", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet('https://raw.githubusercontent.com/acsu123/HOHO_H/main/Loading_UI'))()
    else
        print("Toggle Off")
    end
end)

Tab3Section:NewToggle("ZENHUB X BLOX FRUIT", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet("https://raw.githubusercontent.com/Kaizenofficiall/ZenHub/main/Loader", true))() 		
    else
        print("Toggle Off")
    end
end)

Tab3Section:NewToggle("ZAQUE HUB X BLOX FRUIT", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet("https://raw.githubusercontent.com/Mei2232/ZaqueHub/main/Zaque%20Hub"))() 		
    else
        print("Toggle Off")
    end
end)

Tab3Section:NewToggle("PLAYBACK X BLOX FRUIT", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet("https://raw.githubusercontent.com/NeaPchX2/Playback-X-HUB/main/PlaybackXHub.lua"))()
    else
        print("Toggle Off")
    end
end)

Tab3Section:NewToggle("BLACK TRAP X BLOX FRUIT", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet("https://raw.githubusercontent.com/Sky2836/BT/main/BYPASS/BLACKTRAP"))()
    else
        print("Toggle Off")
    end
end)

Tab3Section:NewToggle("ANTI AFK", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet(('https://pastebin.com/raw/TsrUjuaf')))() 		
    else
        print("Toggle Off")
    end
end)

Tab3Section:NewToggle("AUTO CHEST V1", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet("https://raw.githubusercontent.com/markxd07/get-chest-bf/main/luxuryhub.lua"))() 		
    else
        print("Toggle Off")
    end
end)

Tab3Section:NewToggle("AUTO CHEST V2", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet('https://raw.githubusercontent.com/SHAREHACK/bloxfruit/main/autochest'))()
    else
        print("Toggle Off")
    end
end)

Tab3Section:NewToggle("AUTO CHEST V3", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet("https://raw.githubusercontent.com/IceMael7/NewIceHub/main/Brookhaven"))() 		
    else
        print("Toggle Off")
    end
end)

Tab4Section:NewToggle("THUNDER Z X BLOX FRUIT", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet("https://raw.githubusercontent.com/ThunderZ-HUB/HUB/main/Aimbot"))()
    else
        print("Toggle Off")
    end
end)

Tab5Section:NewToggle("BÀN PHÍM PC", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet(('https://raw.githubusercontent.com/advxzivhsjjdhxhsidifvsh/mobkeyboard/main/mob.txt'),true))() 		
    else
        print("Toggle Off")
    end
end)

Tab6Section:NewToggle("FLY", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet(('https://pastebin.com/raw/aBgJfw6Z')))() 		
    else
        print("Toggle Off")
    end
end)

Tab6Section:NewSlider("CHẠY NHANH", "KÉO THANH Ở GIỮA ĐỂ CHẠY NHANH HOẶC CHẠY CHẬM LƯU Ý: TỶ LỆ BAN RẤT CAO", 500, 0, function(s) -- 500 (MaxValue) | 0 (MinValue)
    game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = s
end)

Tab6Section:NewSlider("FOV", "KÉO THANH Ở GIỮA ĐỂ GÓC NHÌN XA HƠN HOẶC NHỎ HƠN", 125, 0, function(s) -- 125 (MaxValue) | 0 (MinValue)
    game.Workspace.Camera.FieldOfView = s
end)

Tab7Section:NewToggle("SHOW FPS", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet(('https://pastebin.com/raw/43gDZMAw')))() 		
    else
        print("Toggle Off")
    end
end)

Tab7Section:NewToggle("TĂNG FPS V1", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet(('https://pastebin.com/raw/gHq1GzPJ')))() 		
    else
        print("Toggle Off")
    end
end)

Tab7Section:NewToggle("TĂNG FPS V2", "LƯU Ý: LỖI MAP KHÔNG NÊN SỬ DỤNG", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet(('https://pastebin.com/raw/TxGL7QpY')))() 		
    else
        print("Toggle Off")
    end
end)

Tab7Section:NewToggle("TĂNG FPS V3", "ToggleInfo", function(state)
    if state then
        print("Toggle On")
loadstring(game:HttpGet(('https://pastebin.com/raw/c2r8gi0c')))() 		
    else
        print("Toggle Off")
    end
end)

Tab1Section:NewLabel("16/1/2023: AUTO CHEST V3 KHÔNG HOẠT ĐỘNG")
Tab1Section:NewLabel("16/1/2023: CHỈNH SỬA TÊN & THÊM TÍNH NĂNG")
Tab1Section:NewLabel("15/1/2023: ĐÃ THÊM HACK FARM & SĂN BOUTY & TĂNG FPS V3 & TĂNG TẦM NHÌN")
Tab1Section:NewLabel("13/1/2023: SỮA LỖI")
Tab1Section:NewLabel("12/1/2023: SỬA & THÊM TÍNH NĂNG")
Tab1Section:NewLabel("11/1/2023: SỬA & THÊM TẤT CẢ TỪ GIAO DIỆN BẢN V1 QUA DIỆN BẢN V2")
Tab1Section:NewLabel("12/1/2023: NGÀY RA MẮT GIAO DIỆN V2")
